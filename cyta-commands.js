// Generated by CoffeeScript 1.12.4
var colors, initCommands;

colors = require('colors');

initCommands = function(program) {
  return program.command('cyta').description('CYTA Telephone Network related utilities').option('--show-sum', 'Show amount due').action(function(options) {
    var CYTA;
    CYTA = require('./cyta');
    return Helpers.configurator().then(function(res) {
      Helpers.loadConfig({
        config: "cyta"
      });
      if (options.showSum) {
        return CYTA.showSum({
          username: config.cyta.username,
          password: config.cyta.password
        }).then(console.log)["catch"](console.log);
      }
    });
  });
};

module.exports = initCommands;
